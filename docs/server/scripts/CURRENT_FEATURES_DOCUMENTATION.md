# 현재 사용 중인 클러스터링 피처 정리 문서

## 📊 개요

현재 사전 클러스터링에 사용되는 **6개 피처**의 의미, 계산 방법, 데이터 소스, 사용 목적을 정리합니다.

---

## 🎯 피처 목록 (6개)

| 순번 | 피처명 | 타입 | 한글명 | 카테고리 |
|------|--------|------|--------|----------|
| 1 | `age_scaled` | 연속형 (표준화) | 연령 | 인구통계 |
| 2 | `Q6_scaled` | 연속형 (표준화) | 소득 | 인구통계 |
| 3 | `education_level_scaled` | 연속형 (표준화) | 학력 | 인구통계 |
| 4 | `Q8_count_scaled` | 연속형 (표준화) | 전자제품 수 | 소비/제품 |
| 5 | `Q8_premium_index` | 연속형 (0~1) | 프리미엄 지수 | 소비/제품 |
| 6 | `is_premium_car` | 이진형 (0/1) | 프리미엄 차량 보유 | 소비/제품 |

---

## 📋 상세 설명

### 1. `age_scaled` - 연령 (표준화)

#### 의미
- **원본 변수**: `age` (실제 나이, 정수)
- **변환 방법**: StandardScaler를 사용한 Z-score 정규화
- **값의 범위**: 평균 0, 표준편차 1인 정규분포 (음수/양수 가능)
- **계산식**: `(age - mean(age)) / std(age)`

#### 데이터 소스
- **원본 테이블**: `welcome_1st.birth_year`
- **계산 과정**:
  1. `birth_year`에서 출생연도 추출
  2. `EXTRACT(YEAR FROM AGE(CURRENT_DATE, MAKE_DATE(birth_year, 1, 1)))`로 나이 계산
  3. 결측치는 중앙값(median)으로 대체
  4. StandardScaler로 표준화

#### 사용 목적
- **클러스터링**: 연령대별 구분 (10대, 20대, 30대, 40대, 50대, 60대+)
- **비즈니스 인사이트**: 세대별 소비 패턴, 라이프스타일 차이 분석
- **표준화 이유**: 다른 피처(소득, 학력 등)와 스케일을 맞춰 클러스터링 성능 향상

#### 예시 값
- `-1.5`: 평균보다 1.5 표준편차 낮은 연령 (젊은 층)
- `0.0`: 평균 연령
- `+1.5`: 평균보다 1.5 표준편차 높은 연령 (중장년층)

---

### 2. `Q6_scaled` - 소득 (표준화)

#### 의미
- **원본 변수**: `Q6_income` (소득액, 만원 단위)
- **변환 방법**: StandardScaler를 사용한 Z-score 정규화
- **값의 범위**: 평균 0, 표준편차 1인 정규분포 (음수/양수 가능)
- **계산식**: `(Q6_income - mean(Q6_income)) / std(Q6_income)`

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'income_personal'` 또는 `income_household`
- **우선순위**:
  1. `income_personal` (개인 소득) 우선
  2. 없으면 `income_household` (가구 소득) 사용
- **계산 과정**:
  1. `welcome_2nd.data` JSONB에서 소득 정보 추출
  2. 결측치는 중앙값(median)으로 대체
  3. StandardScaler로 표준화

#### 사용 목적
- **클러스터링**: 소득 수준별 구분 (저소득, 중소득, 고소득)
- **비즈니스 인사이트**: 소득 수준에 따른 소비 패턴, 프리미엄 제품 선호도 분석
- **표준화 이유**: 연령, 학력 등과 스케일을 맞춰 클러스터링 성능 향상

#### 예시 값
- `-1.0`: 평균보다 1 표준편차 낮은 소득 (저소득층)
- `0.0`: 평균 소득
- `+2.0`: 평균보다 2 표준편차 높은 소득 (고소득층)

---

### 3. `education_level_scaled` - 학력 (표준화)

#### 의미
- **원본 변수**: `Q7` (학력 수준, 정수)
- **변환 방법**: StandardScaler를 사용한 Z-score 정규화
- **값의 범위**: 평균 0, 표준편차 1인 정규분포 (음수/양수 가능)
- **계산식**: `(Q7 - mean(Q7)) / std(Q7)`

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q7'`
- **학력 수준**: 정수 값 (예: 1=중졸, 2=고졸, 3=전문대졸, 4=대졸, 5=대학원 이상)
- **계산 과정**:
  1. `welcome_2nd.data` JSONB에서 Q7 값 추출
  2. 결측치는 중앙값(median)으로 대체
  3. StandardScaler로 표준화

#### 사용 목적
- **클러스터링**: 학력 수준별 구분 (저학력, 중학력, 고학력)
- **비즈니스 인사이트**: 학력에 따른 소비 패턴, 직업, 소득 수준과의 상관관계 분석
- **표준화 이유**: 연령, 소득 등과 스케일을 맞춰 클러스터링 성능 향상

#### 예시 값
- `-1.2`: 평균보다 1.2 표준편차 낮은 학력 (저학력)
- `0.0`: 평균 학력
- `+1.8`: 평균보다 1.8 표준편차 높은 학력 (고학력)

---

### 4. `Q8_count_scaled` - 전자제품 수 (표준화)

#### 의미
- **원본 변수**: `Q8_count` (보유한 전자제품 개수, 정수)
- **변환 방법**: StandardScaler를 사용한 Z-score 정규화
- **값의 범위**: 평균 0, 표준편차 1인 정규분포 (음수/양수 가능)
- **계산식**: `(Q8_count - mean(Q8_count)) / std(Q8_count)`

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->'Q8'` (JSONB 배열)
- **계산 과정**:
  1. `welcome_2nd.data->'Q8'`에서 전자제품 배열 추출
  2. 배열 길이를 계산하여 `Q8_count` 생성
  3. 결측치는 0으로 대체
  4. StandardScaler로 표준화

#### 전자제품 범위
- **Q8 항목**: 다양한 전자제품 (예: 스마트폰, 노트북, 태블릿, TV, 냉장고, 세탁기, 에어컨, 로봇청소기 등)
- **개수**: 한 패널이 보유한 전자제품의 총 개수

#### 사용 목적
- **클러스터링**: 전자제품 보유 수준별 구분 (적게 보유, 보통, 많이 보유)
- **비즈니스 인사이트**: 기술 친화도, 디지털 라이프스타일, 소비 여력 지표
- **표준화 이유**: 다른 피처와 스케일을 맞춰 클러스터링 성능 향상

#### 예시 값
- `-0.8`: 평균보다 0.8 표준편차 적게 보유 (적게 보유)
- `0.0`: 평균 보유 수
- `+1.5`: 평균보다 1.5 표준편차 많이 보유 (많이 보유)

---

### 5. `Q8_premium_index` - 프리미엄 지수

#### 의미
- **계산 변수**: `Q8_premium_count` (프리미엄 제품 수), `Q8_count` (전체 제품 수)
- **변환 방법**: 비율 계산 (0~1 범위)
- **값의 범위**: 0.0 ~ 1.0
- **계산식**: `Q8_premium_count / (Q8_count + 1)`
  - `+1`은 0으로 나누기 방지용

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->'Q8'` (JSONB 배열)
- **프리미엄 제품 정의**: Q8 배열에 포함된 특정 제품 ID
  - 예시: `[3, 9, 18, 20, 22, 25]` (프리미엄 제품 번호)
- **계산 과정**:
  1. Q8 배열에서 프리미엄 제품 ID가 포함된 개수 계산 → `Q8_premium_count`
  2. 전체 제품 수 계산 → `Q8_count`
  3. `Q8_premium_count / (Q8_count + 1)` 계산

#### 프리미엄 제품 예시
- 고가 스마트폰 (아이폰, 갤럭시 S/Note 시리즈)
- 프리미엄 노트북 (맥북, 고사양 노트북)
- 프리미엄 가전제품 (고가 냉장고, 세탁기 등)

#### 사용 목적
- **클러스터링**: 프리미엄 소비 성향별 구분 (저프리미엄, 중프리미엄, 고프리미엄)
- **비즈니스 인사이트**: 프리미엄 제품 선호도, 소비 여력, 라이프스타일 수준 지표
- **비율 사용 이유**: 전체 제품 수와 무관하게 프리미엄 비율을 비교하기 위함

#### 예시 값
- `0.0`: 프리미엄 제품 없음 (0개)
- `0.3`: 전체 제품의 30%가 프리미엄 제품
- `0.8`: 전체 제품의 80%가 프리미엄 제품 (고프리미엄 소비자)
- `1.0`: 모든 제품이 프리미엄 제품 (100%)

---

### 6. `is_premium_car` - 프리미엄 차량 보유 여부

#### 의미
- **원본 변수**: `Q8` 배열 (전자제품 배열, 차량 포함)
- **변환 방법**: 이진 변수 (Boolean → 0/1)
- **값의 범위**: 0 또는 1
- **계산식**: `any(item in [9, 10] for item in Q8_list)`
  - Q8 배열에 차량 관련 제품 ID (예: 9, 10)가 포함되어 있으면 1, 없으면 0

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->'Q8'` (JSONB 배열)
- **차량 제품 ID**: 예시로 `[9, 10]` (실제 ID는 프로젝트에 따라 다를 수 있음)
- **계산 과정**:
  1. Q8 배열을 파싱하여 리스트로 변환
  2. 차량 관련 제품 ID가 포함되어 있는지 확인
  3. 있으면 `True` (1), 없으면 `False` (0)
  4. 결측치는 `False` (0)로 대체

#### 차량 제품 예시
- 프리미엄 자동차 (고가 차량)
- 전기차, 하이브리드차 등

#### 사용 목적
- **클러스터링**: 프리미엄 차량 보유 여부별 구분 (보유 vs 미보유)
- **비즈니스 인사이트**: 프리미엄 소비 성향, 소득 수준, 라이프스타일 지표
- **이진 변수 사용 이유**: 차량 보유 여부는 명확한 이진 구분이 가능하고, 클러스터 구분에 효과적

#### 예시 값
- `0`: 프리미엄 차량 미보유
- `1`: 프리미엄 차량 보유

---

## 🔄 피처 생성 파이프라인

### 1단계: 원본 데이터 추출
```
welcome_1st (기본 정보)
  ├─ birth_year → age
  └─ gender, location

welcome_2nd (설문 응답)
  ├─ data->>'Q6' → Q6_income
  ├─ data->>'Q7' → Q7 (학력)
  └─ data->'Q8' → Q8_list (전자제품 배열)
```

### 2단계: 기본 변수 생성
```
age (나이)
Q6_income (소득, 만원)
Q7 (학력 수준)
Q8_count (전자제품 개수)
Q8_premium_count (프리미엄 제품 개수)
is_premium_car (프리미엄 차량 보유 여부)
```

### 3단계: 표준화 및 지수 계산
```
age → StandardScaler → age_scaled
Q6_income → StandardScaler → Q6_scaled
Q7 → StandardScaler → education_level_scaled
Q8_count → StandardScaler → Q8_count_scaled
Q8_premium_count / (Q8_count + 1) → Q8_premium_index
is_premium_car → Boolean → 0/1
```

### 4단계: 최종 피처 매트릭스
```
[age_scaled, Q6_scaled, education_level_scaled, 
 Q8_count_scaled, Q8_premium_index, is_premium_car]
```

---

## 📊 피처 통계 정보

### 표준화된 피처 (StandardScaler)
- **평균**: 0 (또는 0에 매우 가까운 값)
- **표준편차**: 1 (또는 1에 매우 가까운 값)
- **분포**: 정규분포에 가까운 형태

### 비표준화 피처
- **`Q8_premium_index`**: 0.0 ~ 1.0 범위, 평균 약 0.2~0.3 예상
- **`is_premium_car`**: 0 또는 1, 평균 약 0.1~0.2 (10~20% 보유율 예상)

---

## 🎯 클러스터링에서의 역할

### 인구통계 차원 (3개)
- `age_scaled`: 연령대별 구분
- `Q6_scaled`: 소득 수준별 구분
- `education_level_scaled`: 학력 수준별 구분

### 소비 패턴 차원 (3개)
- `Q8_count_scaled`: 전자제품 보유 수준별 구분
- `Q8_premium_index`: 프리미엄 소비 성향별 구분
- `is_premium_car`: 프리미엄 차량 보유 여부별 구분

---

## ⚠️ 주의사항

### 1. 결측치 처리
- **표준화 피처**: 중앙값(median)으로 대체 후 표준화
- **Q8 관련**: 결측치는 0 또는 False로 대체
- **영향**: 결측치가 많으면 클러스터링 품질 저하 가능

### 2. 스케일 차이
- **표준화 피처**: 모두 동일한 스케일 (평균 0, 표준편차 1)
- **비표준화 피처**: `Q8_premium_index` (0~1), `is_premium_car` (0/1)
- **영향**: K-Means 클러스터링 시 스케일 차이가 클러스터 구분에 영향을 줄 수 있음

### 3. 상관관계
- **높은 상관관계 가능성**:
  - `Q6_scaled` ↔ `Q8_premium_index`: 소득과 프리미엄 소비 성향
  - `Q6_scaled` ↔ `is_premium_car`: 소득과 프리미엄 차량 보유
  - `education_level_scaled` ↔ `Q6_scaled`: 학력과 소득
- **영향**: 다중공선성으로 인해 일부 피처의 기여도가 낮을 수 있음

### 4. 프리미엄 제품 정의
- **현재**: 하드코딩된 제품 ID 리스트 사용
- **문제**: 제품 ID가 변경되거나 정의가 모호할 수 있음
- **개선 필요**: 프리미엄 제품 정의를 명확히 하고, 동적으로 관리

---

## 📈 개선 가능성

### 1. 피처 수 확장
- 현재 6개 → 12~15개로 확장 가능
- 라이프스타일, 가족, 지역 변수 추가

### 2. 파생변수 생성
- 상호작용 변수: `age × income`, `education × income`
- 복합 지수: `premium_lifestyle_index`, `socioeconomic_index`

### 3. 가중치 적용
- 도메인 지식 기반: 소득, 프리미엄 지수에 높은 가중치
- 통계적 가중치: 분산, 독립성 기반

---

## 📚 참고 자료

- **데이터 소스**: `server/scripts/generate_welcome_joined_csv.py`
- **전처리 로직**: `server/app/clustering/data_preprocessor.py`
- **피처 매핑**: `server/app/clustering/compare.py` (FEATURE_NAME_MAP)
- **클러스터링 설정**: `server/app/clustering/config.py`

---

## 📦 활용 가능한 추가 피처 목록

현재 사용 중인 6개 피처 외에 클러스터링에 활용 가능한 피처들을 카테고리별로 정리합니다.

---

## 🏙️ 인구통계 추가 피처

### 1. `is_metro` - 수도권 거주 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 수도권 거주 시 1, 아니면 0
- **정의**: 서울, 경기, 인천 거주 여부

#### 데이터 소스
- **원본 테이블**: `welcome_1st.location` 또는 `region_category`
- **계산 방법**: 주소/지역 정보에서 수도권 여부 판단

#### 사용 목적
- **클러스터링**: 지역별 구분 (수도권 vs 비수도권)
- **비즈니스 인사이트**: 지역별 소비 패턴, 생활 수준 차이 분석
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - 지역 특성이 클러스터 구분에 유용

#### 예시 값
- `0`: 비수도권 거주
- `1`: 수도권 거주

---

### 2. `is_metro_city` - 광역시 거주 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 광역시 거주 시 1, 아니면 0
- **정의**: 부산, 대구, 인천, 광주, 대전, 울산 등 광역시 거주 여부

#### 데이터 소스
- **원본 테이블**: `welcome_1st.location` 또는 `region_category`
- **계산 방법**: 주소/지역 정보에서 광역시 여부 판단

#### 사용 목적
- **클러스터링**: 지역 규모별 구분 (광역시 vs 중소도시 vs 농어촌)
- **비즈니스 인사이트**: 도시 규모에 따른 소비 패턴 차이 분석
- **활용 가능성**: ⭐⭐⭐ (보통) - `is_metro`와 중복 가능성

---

### 3. `region_category` - 지역 카테고리

#### 의미
- **타입**: 범주형 (문자열)
- **값**: 예) "수도권", "영남권", "호남권", "충청권", "강원권", "제주권"
- **정의**: 지역을 대권역으로 분류

#### 데이터 소스
- **원본 테이블**: `welcome_1st.location`
- **계산 방법**: 주소 정보를 기반으로 대권역 분류

#### 사용 목적
- **클러스터링**: 지역별 구분 (원-핫 인코딩 필요)
- **비즈니스 인사이트**: 지역 문화, 소비 패턴 차이 분석
- **활용 가능성**: ⭐⭐⭐ (보통) - 범주형 변수는 원-핫 인코딩 필요

---

### 4. `is_college_graduate` - 대졸 이상 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 대졸 이상 학력 시 1, 아니면 0
- **정의**: 4년제 대학 졸업 이상 여부

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q7'` (학력 정보)
- **계산 방법**: Q7 값이 대졸 이상(예: 4 이상)이면 1, 아니면 0

#### 사용 목적
- **클러스터링**: 학력 수준별 구분 (고학력 vs 저학력)
- **비즈니스 인사이트**: 학력에 따른 소비 패턴, 직업, 소득 수준 차이 분석
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - `education_level_scaled`와 보완적 관계

---

## 👨‍👩‍👧‍👦 가족/생활 추가 피처

### 5. `has_children` - 자녀 유무

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 자녀가 있으면 1, 없으면 0
- **정의**: 자녀 보유 여부

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data` (설문 응답)
- **계산 방법**: 자녀 관련 질문 응답에서 자녀 유무 판단

#### 사용 목적
- **클러스터링**: 가족 구성별 구분 (자녀 있음 vs 없음)
- **비즈니스 인사이트**: 자녀 유무에 따른 소비 패턴, 라이프스타일 차이 분석
- **활용 가능성**: ⭐⭐⭐⭐⭐ (매우 높음) - 가족 생애주기 구분에 핵심

#### 예시 값
- `0`: 자녀 없음
- `1`: 자녀 있음

---

### 6. `children_category_ordinal` - 자녀 수 (순서형)

#### 의미
- **타입**: 순서형 (정수)
- **값**: 예) 0=없음, 1=1명, 2=2명, 3=3명 이상
- **정의**: 자녀 수를 순서형으로 표현

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data` (설문 응답)
- **계산 방법**: 자녀 수를 카테고리로 변환

#### 사용 목적
- **클러스터링**: 자녀 수별 구분 (표준화 가능)
- **비즈니스 인사이트**: 자녀 수에 따른 소비 패턴, 가구 소비 여력 차이 분석
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - `has_children`보다 세분화된 정보

---

### 7. `family_type` - 가족 유형

#### 의미
- **타입**: 범주형 (문자열)
- **값**: 예) "미혼", "기혼_자녀없음", "기혼_자녀있음", "한부모", "기타"
- **정의**: 가족 구성 유형

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data` (설문 응답)
- **계산 방법**: 결혼 여부, 자녀 유무 등을 조합하여 가족 유형 분류

#### 사용 목적
- **클러스터링**: 가족 유형별 구분 (원-핫 인코딩 필요)
- **비즈니스 인사이트**: 가족 유형에 따른 소비 패턴, 라이프스타일 차이 분석
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - 가족 생애주기 구분에 유용

---

### 8. `family_type_기혼_자녀있음` - 기혼 자녀있음 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 기혼이고 자녀가 있으면 1, 아니면 0
- **정의**: 전형적인 핵가족 형태 여부

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data` (설문 응답)
- **계산 방법**: 결혼 여부와 자녀 유무를 조합

#### 사용 목적
- **클러스터링**: 핵가족 여부별 구분
- **비즈니스 인사이트**: 핵가족의 소비 패턴, 교육비 지출, 가구 소비 여력 분석
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - 특정 가족 유형 구분에 유용

---

## 💼 직업/경제활동 추가 피처

### 9. `is_employed` - 취업 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 취업 중이면 1, 아니면 0
- **정의**: 현재 취업 상태

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q5'` (직업 정보)
- **계산 방법**: 직업 정보에서 취업 여부 판단

#### 사용 목적
- **클러스터링**: 경제활동 상태별 구분 (취업 vs 비취업)
- **비즈니스 인사이트**: 취업 여부에 따른 소득, 소비 패턴 차이 분석
- **활용 가능성**: ⭐⭐⭐ (보통) - 소득과 상관관계 높음

---

### 10. `is_student` - 학생 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 학생이면 1, 아니면 0
- **정의**: 현재 학생 신분 여부

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q5'` (직업 정보)
- **계산 방법**: 직업 정보에서 학생 여부 판단

#### 사용 목적
- **클러스터링**: 학생 여부별 구분
- **비즈니스 인사이트**: 학생의 소비 패턴, 소득 수준, 라이프스타일 차이 분석
- **활용 가능성**: ⭐⭐⭐ (보통) - 특정 연령대에서 유용

---

## 🛍️ 소비/제품 추가 피처

### 11. `is_premium_phone` - 프리미엄 폰 보유 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 프리미엄 폰 보유 시 1, 아니면 0
- **정의**: 고가 스마트폰 보유 여부 (예: 아이폰, 갤럭시 S/Note 시리즈)

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q9'` (스마트폰 정보)
- **계산 방법**: 스마트폰 모델 정보에서 프리미엄 여부 판단

#### 사용 목적
- **클러스터링**: 프리미엄 폰 보유 여부별 구분
- **비즈니스 인사이트**: 프리미엄 폰 선호도, 기술 친화도, 소비 여력 지표
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - `Q8_premium_index`와 보완적 관계

---

### 12. `is_apple_user` - 애플 사용자 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 애플 제품 사용 시 1, 아니면 0
- **정의**: 아이폰, 아이패드, 맥북 등 애플 제품 사용 여부

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q9'` (스마트폰 정보) 또는 `Q8` (전자제품 배열)
- **계산 방법**: 제품 정보에서 애플 제품 포함 여부 판단

#### 사용 목적
- **클러스터링**: 브랜드 선호도별 구분 (애플 vs 비애플)
- **비즈니스 인사이트**: 브랜드 선호도, 프리미엄 소비 성향, 라이프스타일 지표
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - 브랜드 선호도 구분에 유용

---

### 13. `is_samsung_user` - 삼성 사용자 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 삼성 제품 사용 시 1, 아니면 0
- **정의**: 갤럭시, 삼성 가전 등 삼성 제품 사용 여부

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q9'` (스마트폰 정보) 또는 `Q8` (전자제품 배열)
- **계산 방법**: 제품 정보에서 삼성 제품 포함 여부 판단

#### 사용 목적
- **클러스터링**: 브랜드 선호도별 구분 (삼성 vs 비삼성)
- **비즈니스 인사이트**: 브랜드 선호도, 국산 브랜드 선호도 지표
- **활용 가능성**: ⭐⭐⭐ (보통) - `is_apple_user`와 상호 보완적

---

### 14. `has_car` - 차량 보유 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 차량 보유 시 1, 없으면 0
- **정의**: 자동차 보유 여부 (프리미엄 여부 무관)

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q8'` (전자제품 배열) 또는 `Q11` (차량 정보)
- **계산 방법**: Q8 배열에 차량 관련 제품 ID 포함 여부 또는 Q11 정보 확인

#### 사용 목적
- **클러스터링**: 차량 보유 여부별 구분
- **비즈니스 인사이트**: 차량 보유에 따른 소비 패턴, 생활 수준, 이동성 지표
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - `is_premium_car`와 보완적 관계

---

### 15. `Q8_premium_count` - 프리미엄 제품 수

#### 의미
- **타입**: 연속형 (정수)
- **값**: 보유한 프리미엄 제품의 개수
- **정의**: 프리미엄 제품 ID가 포함된 개수

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q8'` (전자제품 배열)
- **계산 방법**: Q8 배열에서 프리미엄 제품 ID 개수 계산
- **프리미엄 제품 ID**: `[3, 9, 18, 20, 22, 25]` (예시)

#### 사용 목적
- **클러스터링**: 프리미엄 제품 보유 수준별 구분 (표준화 필요)
- **비즈니스 인사이트**: 프리미엄 제품 보유 수, 소비 여력 지표
- **활용 가능성**: ⭐⭐⭐ (보통) - `Q8_premium_index`와 중복 가능성

---

## 🍺 라이프스타일 추가 피처

### 16. `has_drinking_experience` - 음주 경험 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 음주 경험이 있으면 1, 없으면 0
- **정의**: 과거 또는 현재 음주 경험 여부

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q13'` (음주 정보)
- **계산 방법**: 음주 관련 질문 응답에서 경험 여부 판단

#### 사용 목적
- **클러스터링**: 음주 경험 여부별 구분
- **비즈니스 인사이트**: 음주 경험에 따른 라이프스타일, 소비 패턴 차이 분석
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - 라이프스타일 구분에 핵심

---

### 17. `drinking_types_count` - 음주 유형 수

#### 의미
- **타입**: 연속형 (정수)
- **값**: 마시는 술의 종류 개수 (예: 맥주, 소주, 와인 등)
- **정의**: 다양한 음주 유형을 즐기는 정도

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q13'` (음주 정보)
- **계산 방법**: 음주 유형 질문에서 선택한 항목 개수 계산
- **음주 유형**: 맥주, 소주, 와인, 양주, 막걸리, 저도수, 칵테일 등

#### 사용 목적
- **클러스터링**: 음주 다양성별 구분 (표준화 필요)
- **비즈니스 인사이트**: 음주 다양성, 라이프스타일 수준, 소비 패턴 지표
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - 라이프스타일 구분에 유용

---

### 18. `has_smoking_experience` - 흡연 경험 여부

#### 의미
- **타입**: 이진형 (0/1)
- **값**: 흡연 경험이 있으면 1, 없으면 0
- **정의**: 과거 또는 현재 흡연 경험 여부

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q12'` (흡연 정보)
- **계산 방법**: 흡연 관련 질문 응답에서 경험 여부 판단

#### 사용 목적
- **클러스터링**: 흡연 경험 여부별 구분
- **비즈니스 인사이트**: 흡연 경험에 따른 라이프스타일, 건강 인식, 소비 패턴 차이 분석
- **활용 가능성**: ⭐⭐⭐⭐ (높음) - 라이프스타일 구분에 핵심

---

### 19. `smoking_types_count` - 흡연 유형 수

#### 의미
- **타입**: 연속형 (정수)
- **값**: 사용하는 담배 유형 개수 (예: 일반 담배, 히트, 액상 등)
- **정의**: 다양한 흡연 유형을 사용하는 정도

#### 데이터 소스
- **원본 테이블**: `welcome_2nd.data->>'Q12'` (흡연 정보)
- **계산 방법**: 흡연 유형 질문에서 선택한 항목 개수 계산
- **흡연 유형**: 일반 담배, 히트, 액상, 기타 등

#### 사용 목적
- **클러스터링**: 흡연 다양성별 구분 (표준화 필요)
- **비즈니스 인사이트**: 흡연 다양성, 라이프스타일 수준, 소비 패턴 지표
- **활용 가능성**: ⭐⭐⭐ (보통) - `has_smoking_experience`와 보완적 관계

---

## 📊 피처 활용 우선순위

### ⭐⭐⭐⭐⭐ (매우 높음) - 즉시 활용 권장
1. `has_children` - 자녀 유무
2. `has_drinking_experience` - 음주 경험 여부
3. `has_smoking_experience` - 흡연 경험 여부
4. `is_premium_phone` - 프리미엄 폰 보유 여부
5. `is_apple_user` - 애플 사용자 여부

### ⭐⭐⭐⭐ (높음) - 우선 고려
6. `is_metro` - 수도권 거주 여부
7. `is_college_graduate` - 대졸 이상 여부
8. `children_category_ordinal` - 자녀 수
9. `family_type` - 가족 유형
10. `drinking_types_count` - 음주 유형 수
11. `has_car` - 차량 보유 여부

### ⭐⭐⭐ (보통) - 선택적 활용
12. `is_metro_city` - 광역시 거주 여부
13. `region_category` - 지역 카테고리
14. `is_employed` - 취업 여부
15. `is_student` - 학생 여부
16. `is_samsung_user` - 삼성 사용자 여부
17. `Q8_premium_count` - 프리미엄 제품 수
18. `smoking_types_count` - 흡연 유형 수

---

## 🔧 피처 추가 시 전처리 가이드

### 연속형 변수 → 표준화 필요
- `children_category_ordinal` → `children_category_ordinal_scaled`
- `drinking_types_count` → `drinking_types_count_scaled`
- `smoking_types_count` → `smoking_types_count_scaled`
- `Q8_premium_count` → `Q8_premium_count_scaled`

### 이진형 변수 → 그대로 사용 가능
- `has_children`, `is_metro`, `is_college_graduate`
- `is_premium_phone`, `is_apple_user`, `has_car`
- `has_drinking_experience`, `has_smoking_experience`

### 범주형 변수 → 원-핫 인코딩 필요
- `family_type` → `family_type_미혼`, `family_type_기혼_자녀없음`, `family_type_기혼_자녀있음` 등
- `region_category` → `region_category_수도권`, `region_category_영남권` 등

---

## 📈 확장 피처 조합 예시

### 조합 1: 기본 + 라이프스타일 (9개)
```python
features = [
    # 현재 사용 중 (6개)
    'age_scaled',
    'Q6_scaled',
    'education_level_scaled',
    'Q8_count_scaled',
    'Q8_premium_index',
    'is_premium_car',
    
    # 추가 (3개)
    'has_drinking_experience',
    'has_smoking_experience',
    'has_children',
]
```

### 조합 2: 기본 + 가족 + 지역 (10개)
```python
features = [
    # 현재 사용 중 (6개)
    'age_scaled',
    'Q6_scaled',
    'education_level_scaled',
    'Q8_count_scaled',
    'Q8_premium_index',
    'is_premium_car',
    
    # 추가 (4개)
    'has_children',
    'children_category_ordinal_scaled',
    'is_metro',
    'is_college_graduate',
]
```

### 조합 3: 전체 확장 (15개)
```python
features = [
    # 인구통계 (4개)
    'age_scaled',
    'Q6_scaled',
    'education_level_scaled',
    'is_metro',
    
    # 가족 (2개)
    'has_children',
    'children_category_ordinal_scaled',
    
    # 소비/제품 (4개)
    'Q8_count_scaled',
    'Q8_premium_index',
    'is_premium_car',
    'is_premium_phone',
    
    # 라이프스타일 (3개)
    'has_drinking_experience',
    'drinking_types_count_scaled',
    'has_smoking_experience',
    
    # 기타 (2개)
    'is_college_graduate',
    'is_apple_user',
]
```

---

## ⚠️ 피처 추가 시 주의사항

### 1. 다중공선성 확인
- **높은 상관관계 피처**: 하나만 선택
  - 예: `Q8_premium_index` vs `Q8_premium_count`
  - 예: `is_metro` vs `is_metro_city` vs `region_category`
  - 예: `has_children` vs `children_category_ordinal` vs `family_type`

### 2. 결측치 처리
- **결측치 비율 30% 이상**: 제외 고려
- **결측치 처리 방법**:
  - 연속형: 중앙값(median) 또는 평균(mean)으로 대체
  - 이진형: 0 (False)으로 대체
  - 범주형: "기타" 또는 최빈값으로 대체

### 3. 분산 확인
- **분산 0.01 미만**: 제외 고려 (변동성이 너무 낮음)
- **표준화 후 분산**: 1.0에 가까워야 함

### 4. 피처 수 제한
- **권장 피처 수**: 6~15개
- **최대 피처 수**: 20개 이하 (차원의 저주 방지)
- **최소 피처 수**: 3개 이상

---

## 📚 참고 자료

- **전체 변수 목록**: `server/scripts/CLUSTERING_FEATURES.md`
- **비교 분석 변수**: `server/scripts/COMPARISON_FEATURES.md`
- **개선 전략**: `server/scripts/CLUSTERING_IMPROVEMENT_STRATEGY.md`
- **클러스터링 설정**: `server/app/clustering/config.py`

---

**작성일**: 2025-01-15  
**목적**: 현재 사용 중인 클러스터링 피처의 의미와 계산 방법 명확화, 추가 활용 가능 피처 정리

